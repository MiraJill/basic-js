function concatenateValues(a, b) //declare a function
{
    return a.concat(b); //use ‘concat’ method to join 2 array lists
}
console.log(concatenateValues(['a','b','c'], [1,2,3])); //show output in console

const sayAge = (age) => console.log(`My age is ${age}`);

sayAge(22);